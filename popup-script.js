const signInButton = document.querySelector("#sign-in-button"); signInButton.addEventListener("click", (() => { const e = document.querySelector("#email-input").value, n = document.querySelector("#password-input").value; if (e && n) { const t = document.createElement("div"); t.setAttribute("class", "spinner"), signInButton.textContent = "", signInButton.appendChild(t), chrome.runtime.sendMessage({ message: "sign_in", email: e, password: n }, (e => { "success" === e.message ? window.location.replace("./main.html") : (signInButton.removeChild(t), signInButton.textContent = "Sign In", document.querySelector("#error-info").textContent = e.message) })) } }));