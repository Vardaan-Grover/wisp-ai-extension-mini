async function profilePageFlow() { await delay(5e3); if (document.body.textContent.includes("This account is private")) window.location.assign("https://www.instagram.com/direct/inbox/"); else { const a = await getUnsentUsername(), e = await getDmIdFromUsername(a), t = calculateRandomNumber(15e3, 2e4), n = calculateRandomNumber(2e3, 4e3); await delay(n), chrome.runtime.sendMessage({ message: "watch_posts", maxDuration: t }), await delay(t), window.location.assign(`https://www.instagram.com/direct/t/${e}`) } } profilePageFlow();